# MongoDB (local install — no Docker required)
MONGODB_URI=mongodb://127.0.0.1:27017/scrapper

# API
API_HOST=0.0.0.0
API_PORT=3000
API_CORS_ORIGIN=*

# In-process batch ingest concurrency
INGEST_CONCURRENCY=2
